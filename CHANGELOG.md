# Changelog

All notable changes to Putter get logged here, roughly following [Keep a Changelog](https://keepachangelog.com/) conventions. Version numbers stay at `0.0.0` across every `package.json` until there's an actual first release worth calling `0.1.0` — before that point, dated entries under "Unreleased" are the real record of progress, not a semver number nobody's using yet.

## [Unreleased]

### Added
- **M2: Focus Timer** (2026-09-02) — the first real cross-tool integration (`packages/tool-focus`). Configurable Pomodoro-style timer with pause/resume/cancel, session history, and stats; sessions can optionally link to a Task Manager task via a picker sourced through `ToolManifest.getLinkables()` (a new optional manifest capability — no direct import of `@putter/tool-tasks` anywhere in this package). Completing a session emits `focus:session:completed:v1`; Task Manager subscribes (`packages/tool-tasks/src/focus-integration.ts`) and logs the time on the linked task — the first proof the event-bus architecture actually works across two independent tool packages, not just in theory. Retrofit a typed `EventMap` in `packages/core` for this real event pair, closing the KNOWN-ISSUES entry from M0. Second schema version (`focusSessions`) on the shared `db` singleton, alongside M1's tasks/projects tables. 12 new tests, including a dedicated cross-tool integration test that emits the real event shape with zero import of Focus Timer's code. Test suite: 35 → 47 passing.
- **Self-hosted real fonts** (2026-09-01) — Quicksand, Nunito Sans, and Space Mono (the real Quiet Morning typefaces) are now bundled and self-hosted (`apps/web/public/fonts/`), replacing the system-font fallback. Fonts were provided directly (Google Fonts' own OFL-licensed downloads) and converted from `.ttf` to `.woff2` for size. Real `@font-face` rules in `apps/web/src/index.css`; `woff2` added to the PWA's offline precache glob so fonts work fully offline, not just on a fast repeat visit. Closes the KNOWN-ISSUES entry.
- **M1: Task Manager** (2026-09-01) — the first real tool package (`packages/tool-tasks`). Task CRUD with due dates, priority, and tags; subtasks/checklists; recurring tasks backed by a new shared recurrence engine in `packages/core` (daily/weekly/custom-interval, deliberately narrow scope); projects/lists as a simple grouping mechanism; first real schema version on the shared `db` singleton; a Today widget (due/overdue-today, quick-complete) and a full page view. `ToolManifest` gained an optional `FullView` field, and the app shell gained simple local-state nav (not a router — not needed yet at this scale). 22 new tests (recurrence-engine date math including month/year/leap-day boundaries, task repository CRUD/completion/subtasks). Test suite: 13 → 35 passing.
- **M0 team review fixes** (2026-09-01) — First Full Team Review completed (see `Putter-Team-Reviews.md`, Review 1). Acted on it directly: added `Repository.getWhere()` so tools can query an indexed field instead of reaching past the abstraction; `create()` now uses `add()` so an id collision throws instead of silently overwriting; `update()` strips `id`/`type`/`createdAt` from any patch so those fields can't be silently overwritten; `EventBus.emit()` now isolates a throwing listener instead of blocking delivery to the rest; a new integration test (`db-integration.test.ts`) proves two independently-versioned tool tables coexist correctly on the real shared `db` singleton — the actual pattern every future tool will use, previously untested; a minimal CSP meta tag now enforces the "no third-party network calls" privacy promise at the browser level; CI workflow now sets an explicit least-privilege `permissions: contents: read`.
- **M0 slice 3** (2026-09-01) — Manifest registry (`ManifestRegistry`, `toolRegistry`), tested with zero tools registered. Quiet Morning theme tokens wired into Tailwind v4, matching the real values from the Design Direction doc. 13 passing Vitest tests covering the event bus, registry, and Dexie repository. ESLint (flat config) with real, fixed findings (unused-arg convention, justified `any` usages in the event bus documented inline). GitHub Actions CI workflow (lint → typecheck → test → build on every push).
- **M0 slice 2** (2026-09-01) — Real `packages/core`: shared entity type, versioned event bus, Dexie-backed repository base class including unused-for-now sync-hook stubs (`pushChanges`/`pullChanges`) for a future sync connector. PWA config via `vite-plugin-pwa` (manifest, service worker). `navigator.storage.persist()` handling.
- **M0 slice 1** (2026-09-01) — Initial pnpm workspace: `apps/web` (blank Vite + React + TypeScript) and `packages/core` (placeholder). First commit, first working `pnpm dev`.

### Known gaps
See `KNOWN-ISSUES.md` for the full, maintained list. As of this entry: PWA manifest has no real icons yet, LICENSE has a placeholder copyright name.
